# ── PyTorch — DO NOT install torch from this file directly ───────────────────
#
# The RTX 5050 is a Blackwell GPU (sm_120). Standard pip install gives
# a CPU-only build that ignores your GPU entirely.
#
# Run ONE of these commands FIRST, before pip install -r requirements.txt:
#
# OPTION A — Nightly build (confirmed working on RTX 50-series, Windows):
#   pip install --pre torch torchvision torchaudio --index-url https://download.pytorch.org/whl/nightly/cu128
#
# OPTION B — Stable cu128 (try this first, faster download):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# After installing torch, verify GPU is detected before continuing:
#   python -c "import torch; print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0))"
#   Expected output: True  /  NVIDIA GeForce RTX 5050
#
# If output is False → use Option A (nightly).
# If output is True  → proceed with: pip install -r requirements.txt
#
# torch, torchvision, torchaudio are intentionally excluded below.
# ─────────────────────────────────────────────────────────────────────────────

# ── Data & EDA ────────────────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
opencv-python>=4.7.0       # use opencv-python-headless on servers/Colab
Pillow>=9.5.0
tqdm>=4.65.0
scipy>=1.10.0

# ── Augmentation ──────────────────────────────────────────────────────────────
albumentations>=1.3.0

# ── YOLOv8 ────────────────────────────────────────────────────────────────────
ultralytics>=8.0.0

# ── Faster R-CNN (via torchvision — already included above) ──────────────────
# torchvision models are bundled; no extra install needed.

# ── DETR ──────────────────────────────────────────────────────────────────────
transformers>=4.30.0       # HuggingFace DETR
timm>=0.9.0                # backbone weights used by DETR

# ── Evaluation ────────────────────────────────────────────────────────────────
pycocotools>=2.0.6         # AP@50 / COCO-style metrics
# Windows users: pip install pycocotools-windows

# ── Visualization / Grad-CAM ──────────────────────────────────────────────────
grad-cam>=1.4.6
# or: pip install pytorch-grad-cam

# ── Export ────────────────────────────────────────────────────────────────────
onnx>=1.14.0
onnxruntime>=1.15.0        # CPU inference on exported ONNX model

# ── Notebook ──────────────────────────────────────────────────────────────────
jupyter>=1.0.0
ipywidgets>=8.0.0

# ── Misc ──────────────────────────────────────────────────────────────────────
PyYAML>=6.0
requests>=2.28.0
